## ML Training Skeleton

- Place raw audio under `data/raw/` (do not commit PII).
- Preprocess to `data/processed/` using scripts in `src/preprocess/`.
- Train models with `src/train.py`; export TFLite/ONNX to `../../ml/model/`.

